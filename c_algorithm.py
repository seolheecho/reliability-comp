from no_model import no_reliability_model
from b_SD_data import read_data


def algorithm(formulation):

    data = read
    
    # select which case studies will be solved
    data = ""


    if formulation == 'no':
        m = no_reliability_model(data, renewable)
    



